# Name of your application. Used to uniquely configure containers.
service: sandbox

# Name of the container image (use your-user/app-name on external registries).
image: pako999/surf-store

# Deploy to these servers.
servers:
  web:
    hosts:
      - 51.195.110.240
    options:
      add-host: host.docker.internal:host-gateway
  job:
    hosts:
      - 51.195.110.240
    cmd: bundle exec rake solid_queue:start
    options:
      add-host: host.docker.internal:host-gateway

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# If used with Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
#
# Using an SSL proxy like this requires turning on config.assume_ssl and config.force_ssl in production.rb!
#
# Don't use this when deploying to multiple web servers (then you have to terminate SSL at your load balancer).
#
proxy:
  ssl: true
  host: surf-store.com,www.surf-store.com
  app_port: 3000

# Where you keep your container images.
registry:
  server: ghcr.io
  username: pako999
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - TELEGRAM_BOT_TOKEN
    - TELEGRAM_CHAT_ID
    - R2_ACCESS_KEY_ID
    - R2_SECRET_ACCESS_KEY
    - KLAVIYO_PRIVATE_KEY
    - KLAVIYO_PUBLIC_KEY
    - ANTHROPIC_API_KEY
    - ERACUNI_API_USERNAME
    - ERACUNI_API_SECRET_KEY
    - ERACUNI_API_TOKEN
    - SMTP_ADDRESS
    - SMTP_PORT
    - SMTP_USERNAME
    - SMTP_PASSWORD
    - DHL_API_KEY
    - DHL_SUBSCRIPTION_KEY
  clear:
    SOLID_QUEUE_IN_PUMA: true
    DATABASE_URL: "postgresql://ubuntu:kite@localhost/my_kite_shop_production?host=/var/run/postgresql"
    DB_HOST: "/var/run/postgresql"
    DHL_SENDER_NAME: "Surf-Store d.o.o."
    DHL_SENDER_STREET: "Vojkova cesta 58"
    DHL_SENDER_CITY: "Ljubljana"
    DHL_SENDER_STATE: ""
    DHL_SENDER_POSTCODE: "1000"
    DHL_SENDER_COUNTRY: "SI"
    DHL_SENDER_PHONE: "+386 1 234 5678"
    DHL_SENDER_EMAIL: "info@surf-store.com"
    # Google Analytics / Ads — public IDs (visible in browser source, not secrets)
    GOOGLE_TAG_MANAGER_ID: "GT-K54MS7QS"
    GA4_MEASUREMENT_ID: "G-MLSPRGVMGD"
    GOOGLE_ADS_CONVERSION_ID: "AW-11446870184"
    GOOGLE_ADS_CONVERSION_LABEL: "e9mkCOO-pqccEKjBpdIq"
    HOTJAR_ID: "5062304"

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "sandbox_storage:/rails/storage"
  - "/var/run/postgresql:/var/run/postgresql"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: ruby-4.0.1
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
ssh:
  user: ubuntu

# Use accessory services (secrets come from .kamal/secrets).
# accessories:
#   db:
#     image: mysql:8.0
#     host: 192.168.0.2
#     # Change to 3306 to expose port to the world instead of just local network.
#     port: "127.0.0.1:3306:3306"
#     env:
#       clear:
#         MYSQL_ROOT_HOST: '%'
#       secret:
#         - MYSQL_ROOT_PASSWORD
#     files:
#       - config/mysql/production.cnf:/etc/mysql/my.cnf
#       - db/production.sql:/docker-entrypoint-initdb.d/setup.sql
#     directories:
#       - data:/var/lib/mysql
#   redis:
#     image: valkey/valkey:8
#     host: 192.168.0.2
#     port: 6379
#     directories:
#       - data:/data
